import React from "react";
import { Event } from "@/lib/types";

export const CommentSection = ({ event }: { event: Event }) => {
   return (
      <div className="flex flex-col gap-1">
         <h4 className="text-md font-bold"></h4>
      </div>
   );
};
